# === 工具底座（由 fetch.sh 重建，不入 git，约 3.7G）===
# clone 后跑 `bash fetch.sh` 即可恢复 jdk/arthas/cache
jdk/
arthas/
cache/

# === 编辑器 / 系统临时 ===
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/
*.iml

# === 批量验证报告产出（不进 git）===
reports/

# fetch 自动生成，内容依赖本地选的版本/架构，与 arthas/jdk/cache 同类不入 git
MANIFEST.txt
